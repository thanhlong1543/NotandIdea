version: "3"

services:
  mysqldb:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: ambi
    ports:
    - 3306:3306
    volumes:
      - data:/var/lib/mysql

volumes:
  data:

